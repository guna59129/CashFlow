spring.application.name=money
server.port=8080
 
# ===============================
# Database Configuration (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/money?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Guna2854@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Removed explicit dialect to fix the HHH90000025 warning in your logs
spring.jpa.open-in-view=false
 
# SQL Init
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true
 
# ===============================
# Eureka Configuration (DISABLED)
# ===============================
# This line prevents the app from trying to connect to a Discovery Server
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false